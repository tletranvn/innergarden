<nav class="navbar navbar-expand-lg navbar-dark custom-navbar" role="navigation" aria-label="Navigation principale">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">Inner Garden</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Afficher ou masquer le menu de navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto"> {# ms-auto pour aligner à droite #}
                <li class="nav-item">
                    <a class="nav-link{% if app.request.get('_route') == 'app_home' %} active{% endif %}"
                       {% if app.request.get('_route') == 'app_home' %}aria-current="page"{% endif %}
                       href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link{% if app.request.get('_route') starts with 'articles_' %} active{% endif %}"
                       {% if app.request.get('_route') starts with 'articles_' %}aria-current="page"{% endif %}
                       href="{{ path('articles_list') }}">Articles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link{% if app.request.get('_route') == 'app_contact' %} active{% endif %}"
                       {% if app.request.get('_route') == 'app_contact' %}aria-current="page"{% endif %}
                       href="{{ path('app_contact') }}">Contact</a>
                </li>
                {# Logique de connexion/déconnexion/pseudo et bouton "Inscription" #}
                {% if app.user %}
                    {# Si l'utilisateur est connecté #}

                    {# Lien Dashboard Admin (uniquement pour les admins) - RGAA 1.2 #}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link text-warning{% if app.request.get('_route') == 'admin_dashboard' %} active{% endif %}"
                               {% if app.request.get('_route') == 'admin_dashboard' %}aria-current="page"{% endif %}
                               href="{{ path('admin_dashboard') }}">
                                <i class="bi bi-speedometer2" aria-hidden="true"></i> Dashboard Admin
                            </a>
                        </li>
                    {% endif %}
                    
                    <li class="nav-item">
                        <span class="nav-link text-white">Bonjour, <strong>{{ app.user.pseudo }}</strong> !</span>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-sm btn-outline-light ms-2" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                {% else %}
                    {# Si l'utilisateur n'est PAS connecté #}
                    <li class="nav-item">
                        <a class="btn btn-sm btn-outline-light" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        {# Nouveau bouton pour créer un compte #}
                        <a class="btn btn-sm btn-outline-light ms-2" href="{{ path('app_register') }}">Inscription</a>
                    </li>
                {% endif %}
                {# Fin de la logique #}
            </ul>
        </div>
    </div>
</nav>
