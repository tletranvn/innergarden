# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
    secret: '%env(APP_SECRET)%'

    # Note that the session will be started ONLY if you read or write from it.
    session:
        # Use Redis or database for session storage in production
        # For Heroku, we'll configure this to be more reliable
        cookie_secure: auto
        cookie_samesite: lax
        cookie_lifetime: 3600
        gc_maxlifetime: 3600
        # Store sessions in filesystem by default (dev)
        handler_id: null
        
    csrf_protection:
        enabled: true

    #esi: true
    #fragments: true
        
# Désactive le support de Turbo (si UX Turbo est installé)
#    turbo:
#        enabled: false