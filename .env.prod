# Configuration spécifique pour Heroku avec MySQL et MongoDB
APP_ENV=prod
APP_DEBUG=false
APP_SECRET=change-this-secret-key-in-production

# Base de données MySQL (JawsDB ou ClearDB)
# DATABASE_URL sera automatiquement configuré par Heroku à partir de JAWSDB_URL ou CLEARDB_DATABASE_URL

# Base de données MongoDB (MongoDB Atlas)
# MONGODB_URL sera automatiquement configuré par Heroku à partir de MONGODB_URI

# Configuration pour les uploads (stockage temporaire sur Heroku)
UPLOADS_DIR=/tmp/uploads

# Configuration du mailer (utiliser SendGrid ou autre service)
MAILER_DSN=smtp://username:password@smtp.sendgrid.net:587
